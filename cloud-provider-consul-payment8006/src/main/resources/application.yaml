server:
  port: 8006
spring:
  application:
    name: cloud-payment-service
  cloud:
    consul:
      # consul地址端口
      host: localhost
      port: 8500
      discovery:
        hostname: 127.0.0.1
        service-name: ${spring.application.name}
